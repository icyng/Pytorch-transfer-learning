[project]
name = "final"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "icyng", email = "take523oliver@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.3.5",
    "torch>=2.9.1",
    "torchvision>=0.24.1",
]

[project.scripts]
final = "final:main"

[build-system]
requires = ["uv_build>=0.9.4,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.14.9",
]